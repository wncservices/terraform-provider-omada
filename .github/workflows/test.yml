name: Tests

on:
  pull_request:
    paths-ignore:
      - "README.md"
  push:
    branches:
      - main
    paths-ignore:
      - "README.md"

permissions:
  contents: read

env:
  # Keep in sync with the version validated locally + the .golangci.yml format.
  GOLANGCI_LINT_VERSION: v2.12.2

jobs:
  build:
    name: Build & unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - run: go mod download
      - run: go build -v ./...
      - name: Unit tests
        run: go test ./... -count=1

  acctest:
    name: Acceptance tests (mock controller)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: latest
          terraform_wrapper: false
      # Acceptance tests spin up an in-process mock Omada controller, so they
      # need no real hardware or secrets — they run on every PR.
      - name: Acceptance tests
        env:
          TF_ACC: "1"
        run: go test ./... -v -count=1 -timeout 30m -run '^TestAcc'

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      # go install verifies the module against the checksum database (GOSUMDB),
      # unlike the curl|sh installer this replaces — same install path make
      # tools/GNUmakefile uses locally, so CI and local dev stay in sync.
      - name: Install golangci-lint
        run: go install "github.com/golangci/golangci-lint/v2/cmd/golangci-lint@${GOLANGCI_LINT_VERSION}"
      - run: "$(go env GOPATH)/bin/golangci-lint run ./..."

      # golangci-lint is not configured with a formatter, so without this an
      # unformatted file merges with a green tick. That is not hypothetical:
      # PR #50 arrived with six whitespace-only lines in internal/omada/acl.go
      # and passed every check.
      #
      # `gofmt -l` prints the files it would change and exits 0 either way, so
      # the output has to be turned into the failure.
      - name: gofmt
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-clean. Run \`make fmt\`:"
            echo "$unformatted"
            exit 1
          fi

  docs:
    name: Docs up to date
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - run: go generate ./...
      - name: Fail if docs/ is stale
        run: |
          git diff --compact-summary --exit-code || \
            (echo "::error::docs/ is out of date — run 'go generate ./...' and commit the result" && exit 1)
