---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_dot1x Resource - omada"
subcategory: ""
description: |-
  Manages site-wide 802.1X port authentication.
  This is a singleton per site — import it with the site name rather than creating it. Attributes left unset are not written and keep whatever the controller already has.
  ~> Enabling 802.1X can lock wired clients off the network. Any device that cannot authenticate against the configured RADIUS server loses its connection, which on a switch uplink can include the controller itself. The RADIUS profile and per-port 802.1X settings are not yet manageable from this provider, so treat enable = true as the last step of a change you have already staged in the controller UI.
---

# omada_dot1x (Resource)

Manages site-wide 802.1X port authentication.

This is a singleton per site — import it with the site name rather than creating it. Attributes left unset are not written and keep whatever the controller already has.

~> **Enabling 802.1X can lock wired clients off the network.** Any device that cannot authenticate against the configured RADIUS server loses its connection, which on a switch uplink can include the controller itself. The RADIUS profile and per-port 802.1X settings are not yet manageable from this provider, so treat `enable = true` as the last step of a change you have already staged in the controller UI.

## Example Usage

```terraform
# Site-wide 802.1X port authentication.
#
# Enabling this disconnects any wired client that cannot authenticate against
# the RADIUS server — which can include the controller itself if it sits behind
# an affected switch port. The RADIUS profile and per-port 802.1X settings are
# not manageable from this provider yet, so stage those in the controller UI
# first and flip `enable` last.
resource "omada_dot1x" "this" {
  enable      = false
  vlan_assign = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `auth_mode` (Number) Authentication mode (controller enum; `1` is the default).
- `auth_type` (Number) Authentication type (controller enum; `1` is the default).
- `enable` (Boolean) Enable 802.1X authentication site-wide.
- `mac_format` (Number) MAC address format used towards the RADIUS server (controller enum).
- `site` (String) Site name. Defaults to the primary site. Changing forces replacement.
- `vlan_assign` (Boolean) Allow RADIUS to assign the client's VLAN.

### Read-Only

- `id` (String) The site ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# 802.1X settings are a singleton per site — import by site name.
terraform import omada_dot1x.this Home
```
