---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_site_settings Resource - omada"
subcategory: ""
description: |-
  Manages site-level settings (a singleton). Currently exposes the device status-LED toggle; other site settings are preserved (read-modify-write). Destroying this resource does not reset anything.
---

# omada_site_settings (Resource)

Manages site-level settings (a singleton). Currently exposes the device status-LED toggle; other site settings are preserved (read-modify-write). Destroying this resource does not reset anything.

## Example Usage

```terraform
resource "omada_site_settings" "this" {
  led_enable = true # device status LEDs on
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `led_enable` (Boolean) Whether device status LEDs are on.

### Optional

- `site` (String) Site name. Defaults to the primary site. Changing forces replacement.

### Read-Only

- `id` (String) The site ID.
