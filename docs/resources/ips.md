---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_ips Resource - omada"
subcategory: ""
description: |-
  Manages the gateway's IPS/IDS (intrusion prevention and detection).
  This is a singleton per site — import it with the site name rather than creating it. Attributes left unset are not written and keep whatever the controller already has.
  ~> This is an active security control. Setting enable = false, lowering protection_level, or switching ips_mode to detection-only changes what the gateway blocks. Signature matching also costs throughput, which is the usual reason people turn it down — decide that deliberately rather than by inheriting a default.
---

# omada_ips (Resource)

Manages the gateway's IPS/IDS (intrusion prevention and detection).

This is a singleton per site — import it with the site name rather than creating it. Attributes left unset are not written and keep whatever the controller already has.

~> **This is an active security control.** Setting `enable = false`, lowering `protection_level`, or switching `ips_mode` to detection-only changes what the gateway blocks. Signature matching also costs throughput, which is the usual reason people turn it down — decide that deliberately rather than by inheriting a default.

## Example Usage

```terraform
# Intrusion prevention / detection on the gateway.
#
# Signature matching costs throughput, which is the usual reason to turn the
# protection level down — make that a deliberate choice rather than a default.
resource "omada_ips" "this" {
  enable           = true
  protection_level = 3
  geo_blocking     = true
}

# What each protection level actually covers is reported by the controller and
# cannot be set here.
output "ips_high_categories" {
  value = omada_ips.this.high_categories
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `custom_categories` (List of Number) Signature category IDs to match when a custom protection level is selected.
- `enable` (Boolean) Whether IPS/IDS is active.
- `geo_blocking` (Boolean) Block traffic by source/destination country (geo-IP filtering).
- `ips_mode` (Number) Controller enum selecting prevention versus detection-only. TP-Link does not document the mapping and it is not safe to infer from a live system, so check the controller UI for which value corresponds to which mode before changing it.
- `protection_level` (Number) Protection level. `1`, `2` and `3` correspond to the `low_categories`, `medium_categories` and `high_categories` sets below; a custom level uses `custom_categories`.
- `site` (String) Site name. Defaults to the primary site. Changing forces replacement.

### Read-Only

- `all_categories` (List of Number) Every signature category the gateway knows about. Read-only.
- `high_categories` (List of Number) Signature categories covered by the high protection level. Read-only.
- `id` (String) The site ID.
- `low_categories` (List of Number) Signature categories covered by the low protection level. Read-only.
- `medium_categories` (List of Number) Signature categories covered by the medium protection level. Read-only.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# IPS settings are a singleton per site — import by site name.
terraform import omada_ips.this Home
```
