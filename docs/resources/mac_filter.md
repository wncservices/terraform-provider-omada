---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_mac_filter Resource - omada"
subcategory: ""
description: |-
  Manages the site-wide MAC filtering switch.
  This is a singleton per site — import it with the site name rather than creating it.
  ~> This is only the master toggle. The filter entries themselves are held elsewhere (per-SSID, via omada_wireless_network.mac_filter_enable, and in filter groups this endpoint does not expose), so turning it on by itself filters nothing. As with omada_mac_auth, remember a MAC address is trivially spoofed — this keeps honest devices off a network, it does not keep attackers off one.
---

# omada_mac_filter (Resource)

Manages the site-wide MAC filtering switch.

This is a singleton per site — import it with the site name rather than creating it.

~> This is only the master toggle. The filter entries themselves are held elsewhere (per-SSID, via `omada_wireless_network.mac_filter_enable`, and in filter groups this endpoint does not expose), so turning it on by itself filters nothing. As with `omada_mac_auth`, remember a MAC address is trivially spoofed — this keeps honest devices off a network, it does not keep attackers off one.

## Example Usage

```terraform
# Site-wide MAC filtering switch.
#
# This is only the master toggle — the entries live per-SSID and in filter
# groups the controller does not expose here, so enabling this alone filters
# nothing.
resource "omada_mac_filter" "this" {
  enable = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `enable` (Boolean) Whether MAC filtering is active site-wide.
- `site` (String) Site name. Defaults to the primary site. Changing forces replacement.

### Read-Only

- `id` (String) The site ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# A singleton per site — import by site name.
terraform import omada_mac_filter.this Home
```
