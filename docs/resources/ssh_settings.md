---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_ssh_settings Resource - omada"
subcategory: ""
description: |-
  Manages SSH access to the site's managed devices (gateway, switches, access points).
  This is a singleton per site — import it with the site name rather than creating it. Attributes left unset are not written and keep whatever the controller already has.
  Enabling SSH opens a login on every adopted device, authenticated with the site's device account. Leave layer3_access off unless you actually need to reach devices from outside their own subnet.
---

# omada_ssh_settings (Resource)

Manages SSH access to the site's managed devices (gateway, switches, access points).

This is a singleton per site — import it with the site name rather than creating it. Attributes left unset are not written and keep whatever the controller already has.

Enabling SSH opens a login on every adopted device, authenticated with the site's device account. Leave `layer3_access` off unless you actually need to reach devices from outside their own subnet.

## Example Usage

```terraform
# SSH access to the managed devices (gateway, switches, APs).
# This does not affect SSH to the controller itself.
resource "omada_ssh_settings" "this" {
  ssh_enable      = false
  ssh_server_port = 22

  # Only reachable from the device's own subnet.
  layer3_access = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `layer3_access` (Boolean) Allow SSH from outside the device's own subnet.
- `site` (String) Site name. Defaults to the primary site. Changing forces replacement.
- `ssh_enable` (Boolean) Enable SSH on managed devices.
- `ssh_server_port` (Number) TCP port the device SSH server listens on. Controller default is `22`.

### Read-Only

- `id` (String) The site ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# SSH settings are a singleton per site — import by site name.
terraform import omada_ssh_settings.this Home
```
