---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_rate_limit_profile Resource - omada"
subcategory: ""
description: |-
  Manages a rate-limit profile: a per-client throughput cap that SSIDs and portal authentication can reference.
  The controller ships a built-in Default profile, marked is_builtin. Manage custom profiles here rather than the built-in one.
  A limit only exists on the controller while its enable flag is set — the document omits download_limit entirely when download_limit_enable is false — so setting a limit without enabling it has no effect, and the provider rejects that at plan time rather than writing a value the controller will drop.
---

# omada_rate_limit_profile (Resource)

Manages a rate-limit profile: a per-client throughput cap that SSIDs and portal authentication can reference.

The controller ships a built-in `Default` profile, marked `is_builtin`. Manage custom profiles here rather than the built-in one.

A limit only exists on the controller while its enable flag is set — the document omits `download_limit` entirely when `download_limit_enable` is false — so setting a limit without enabling it has no effect, and the provider rejects that at plan time rather than writing a value the controller will drop.

## Example Usage

```terraform
# A per-client throughput cap that SSIDs and portal auth can reference.
#
# A limit only exists on the controller while its enable flag is set, so set
# them together — the provider rejects a limit without its flag at plan time
# rather than writing a value the controller would drop.
resource "omada_rate_limit_profile" "guest" {
  name = "guest-capped"

  download_limit_enable = true
  download_limit        = 20000 # controller units; the UI presents Kbps

  upload_limit_enable = true
  upload_limit        = 5000
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Profile name.

### Optional

- `download_limit` (Number) Download cap, in the controller's units (the UI presents Kbps). Only meaningful with `download_limit_enable`.
- `download_limit_enable` (Boolean) Cap download throughput.
- `site` (String) Site name. Defaults to the primary site. Changing forces replacement.
- `upload_limit` (Number) Upload cap, in the controller's units (the UI presents Kbps). Only meaningful with `upload_limit_enable`.
- `upload_limit_enable` (Boolean) Cap upload throughput.

### Read-Only

- `id` (String) The ID of this resource.
- `is_builtin` (Boolean) Whether this is the controller's built-in profile. Read-only.
- `site_id` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# By profile ID, or "<site>/<id>".
terraform import omada_rate_limit_profile.guest 692c13f475ee724076c80d24
```
