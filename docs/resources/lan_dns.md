---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_lan_dns Resource - omada"
subcategory: ""
description: |-
  Manages a local (LAN) DNS record on the Omada controller — a hostname/domain resolved to LAN IP(s) for the selected networks.
---

# omada_lan_dns (Resource)

Manages a local (LAN) DNS record on the Omada controller — a hostname/domain resolved to LAN IP(s) for the selected networks.

## Example Usage

```terraform
resource "omada_lan_dns" "nas" {
  name            = "nas"
  domain          = "nas.wilant.be"
  aliases         = ["storage.wilant.be"]
  ip_addresses    = ["10.10.20.50"]
  lan_network_ids = [omada_network.iot.id] # or existing network IDs
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) The domain/hostname to resolve, e.g. `nas.wilant.be`.
- `lan_network_ids` (List of String) IDs of the LAN networks this record is served on. Must reference existing networks.
- `name` (String) Display name for the record.

### Optional

- `aliases` (List of String) Additional domains that resolve to the same addresses.
- `custom_ttl` (Boolean) Whether a custom TTL is used.
- `enable` (Boolean) Whether the record is enabled.
- `ip_addresses` (List of String) IPv4 addresses the domain resolves to.
- `ipv6_addresses` (List of String) IPv6 addresses the domain resolves to.
- `site` (String) Site name. Defaults to the controller's primary site. Changing this forces replacement.

### Read-Only

- `id` (String) Controller-assigned record ID.
- `site_id` (String) Resolved site ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by record ID (primary site):
terraform import omada_lan_dns.nas 692c13f575ee724076c80d3a

# Or scope to a named site:
terraform import omada_lan_dns.nas 'Home/692c13f575ee724076c80d3a'
```
