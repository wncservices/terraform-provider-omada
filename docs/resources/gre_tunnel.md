---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_gre_tunnel Resource - omada"
subcategory: ""
description: |-
  Manages the site's GRE tunnel setting.
  This is a singleton per site — import it with the site name rather than creating it.
  ~> GRE provides no confidentiality or authentication. It encapsulates, it does not encrypt: anyone on the path can read and modify what the tunnel carries. Use it only over a link that is already protected (IPsec, or a physically trusted circuit), never straight across the internet.
---

# omada_gre_tunnel (Resource)

Manages the site's GRE tunnel setting.

This is a singleton per site — import it with the site name rather than creating it.

~> **GRE provides no confidentiality or authentication.** It encapsulates, it does not encrypt: anyone on the path can read and modify what the tunnel carries. Use it only over a link that is already protected (IPsec, or a physically trusted circuit), never straight across the internet.

## Example Usage

```terraform
# GRE encapsulates but does not encrypt: only enable this over a link that is
# already protected.
resource "omada_gre_tunnel" "this" {
  enable   = false
  ssid_ids = []
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `enable` (Boolean) Whether GRE tunnelling is active.
- `site` (String) Site name. Defaults to the primary site. Changing forces replacement.
- `ssid_ids` (List of String) IDs of the SSIDs carried over the tunnel (see `omada_wireless_network`).

### Read-Only

- `id` (String) The site ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Singleton per site — import by site name.
terraform import omada_gre_tunnel.this Default
```
